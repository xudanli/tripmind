/**
 * 灵感模式 API（向后兼容层）
 * 
 * 本文件已重构为模块化结构，所有功能已迁移到新的目录结构：
 * - apis/inspiration.ts - 编排层
 * - services/intentService.ts - 意图检测服务
 * - services/journeyService.ts - 旅程生成服务
 * - llm/deepseekClient.ts - LLM 客户端
 * - validators/ - 校验层
 * - prompts/ - 提示词构建
 * - utils/ - 工具函数
 * 
 * 此处仅作为向后兼容的导出入口
 * 
 * @deprecated 建议直接使用 @/apis/inspiration 中的模块
 */

// 重新导出新结构的功能，保持向后兼容
export {
  generateInspirationHint,
  detectInspirationIntent,
  generateInspirationJourney
} from '@/apis/inspiration'

// 导出其他可能被使用的函数（如果它们还没有迁移）
// 这些函数暂时保留在原位置，后续会逐步迁移
export { getPersonaVoice, switchPersonaBasedOnEmotion, generatePersonaResponse, generatePsychologicalJourney } from './inspirationAPI.legacy'
