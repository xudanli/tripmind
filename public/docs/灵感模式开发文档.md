# 灵感模式开发文档

## 📋 目录
- [概述](#概述)
- [架构设计](#架构设计)
- [数据流程](#数据流程)
- [核心文件](#核心文件)
- [功能模块](#功能模块)
- [开发指南](#开发指南)

---

## 概述

灵感模式是AI旅行伴侣的核心功能，通过理解用户的情绪状态和旅行意图，生成高度个性化的旅程体验。

### 核心特性
- 🎯 **意图识别**：AI理解用户的情感状态和旅行动机
- 🎨 **动态配置**：根据用户输入生成个性化UI和内容
- 🌊 **五层体验**：从情绪入口到行动转化的完整用户旅程
- 🔄 **实时交互**：用户与AI共创旅行灵感
- 📸 **AI视觉诗**：为图片自动生成诗意描述

---

## 架构设计

### 整体架构

```
用户输入
   ↓
AI意图识别 → 生成灵感数据 → 动态配置生成
   ↓                                    ↓
旅程创建 ← 用户体验五层架构 ← 详情页展示
```

### 文件结构

```
src/
├── views/
│   ├── InspirationView.vue          # 灵感输入页面
│   └── TravelDetailView.vue         # 旅程详情页
├── components/TravelDetail/
│   ├── InspirationHero.vue         # 层1：情绪入口
│   └── ExperienceDay.vue            # 层2-5：灵感体验
├── utils/
│   ├── generateInspirationConfig.ts # 动态配置生成器
│   └── imagePoetryGenerator.ts      # AI视觉诗生成器
├── stores/
│   └── travel.ts                    # 状态管理
└── services/
    └── deepseekAPI.ts               # AI API调用
```

---

## 数据流程

### 1. 用户输入阶段

**文件**: `src/views/InspirationView.vue`

```typescript
// 用户输入监听
watch(inspirationInput, async (newValue) => {
  // 实时生成AI提示
  const hint = await generateInspirationHint(newValue, currentLanguage)
  aiHint.value = hint
})
```

**功能**:
- 实时监听用户输入
- 防抖处理（800ms）
- 调用AI生成个性化提示
- 动态显示AI建议

### 2. AI意图识别

**文件**: `src/services/deepseekAPI.ts`

**支持的意图类型**:
```typescript
intentType: 
  - 'photography_exploration'    // 摄影探索
  - 'emotional_healing'         // 情感疗愈
  - 'mind_healing'              // 心灵疗愈
  - 'extreme_exploration'       // 极限探索
  - 'urban_creation'            // 城市创作
  - 'nature_discovery'          // 自然探索
  - 'cultural_exchange'         // 文化交流
```

**API调用**:
```typescript
await travelStore.generateInspiration(inspirationInput.value)
```

### 3. 动态配置生成

**文件**: `src/utils/generateInspirationConfig.ts`

**核心函数**:

#### Moodboard生成
```typescript
function generateMoodItems(highlights: any[]): Array<{ icon: string; text: string }> {
  // 从highlights提取关键词
  // 生成气泡标签（最多4个字）
  // 为每个关键词匹配图标
}
```

#### 视觉诗模板
```typescript
function generatePoetryTemplates(highlights: any[]): Array<{ poetry: string; tags: string[] }> {
  // 基于highlights生成诗意描述
  // 提取关键词作为标签
}
```

#### AI反馈模板
```typescript
function generateAIFeedbackTemplates(intentType?: string): Array<(input: string) => string> {
  // 根据意图类型生成不同回应模板
  // 摄影类、疗愈类、极限类等
}
```

### 4. 旅程创建

**文件**: `src/views/InspirationView.vue`

```typescript
const createTravel = async () => {
  // 1. 生成动态配置
  const inspirationConfig = generateInspirationConfig(data)
  
  // 2. 创建Travel对象
  const newTravel = travelListStore.createTravel({
    title: data.title || '灵感之旅',
    location: selectedLocation.value || data.location || '待定',
    description: data.subtitle || data.aiMessage,
    mode: 'inspiration',
    data: {
      ...data,
      selectedLocation: selectedLocation.value,
      inspirationConfig
    }
  })
  
  // 3. 跳转到详情页
  router.push(`/travel/${newTravel.id}`)
}
```

### 5. 详情页展示

**文件**: `src/components/TravelDetail/ExperienceDay.vue`

**五层架构**:
1. **情绪入口层** (`InspirationHero.vue`)
   - AI生成的标题和摘要
   - 动态模糊背景
   - 继续延展按钮

2. **灵感景象层** (`ExperienceDay.vue` 中的 `.mood-space`)
   - 浮动关键词气泡
   - AI视觉拼贴
   - 推荐目的地卡片

3. **体验旅程层** (`.journey-flow`)
   - 时间段活动安排
   - AI摄影师笔记
   - 沉浸模式

4. **共创空间层** (`.creative-zone`)
   - 灵感创作区
   - 素材收集区
   - 灵感拼图

5. **回声收尾层** (`.echo-outro`)
   - 旅伴留言区
   - 行动按钮
   - AI终结语

---

## 核心文件

### 1. InspirationView.vue
**职责**: 灵感输入和生成
**关键功能**:
- 用户输入处理
- AI提示生成
- 多目的地选择
- 旅程创建

**关键代码**:
```vue
<template>
  <a-textarea v-model:value="inspirationInput" />
  <a-button @click="handleSubmit">生成灵感</a-button>
</template>

<script setup>
const handleSubmit = async () => {
  await travelStore.generateInspiration(inspirationInput.value)
  // 初始化选中目的地
  if (travelStore.inspirationData?.locations?.length > 0) {
    selectedLocation.value = travelStore.inspirationData.locations[0]
  }
}
</script>
```

### 2. ExperienceDay.vue
**职责**: 五层体验展示
**关键功能**:
- 动态Moodboard显示
- 时间段活动安排
- 沉浸模式
- 图片上传和AI视觉诗

**关键代码**:
```vue
<script setup>
// 从travel.data获取灵感数据
const dayExperience = computed(() => {
  return travel.value?.data?.locationDetails?.[selectedLocationName.value] 
    || travel.value?.data
})

// 动态生成moodboard
const dynamicMoodItems = computed(() => {
  const config = travel.value?.data?.inspirationConfig
  const locationMoodMap = config?.locationMoodMap || {}
  return locationMoodMap[selectedLocationName.value] || []
})

// 沉浸模式
const toggleImmersionMode = () => {
  isImmersionMode.value = !isImmersionMode.value
  if (isImmersionMode.value) {
    activePeriod.value = journeyTimeline.value[0].type
    document.body.style.overflow = 'hidden'
  } else {
    activePeriod.value = null
    document.body.style.overflow = ''
  }
}
</script>
```

### 3. generateInspirationConfig.ts
**职责**: 动态配置生成
**关键函数**:

```typescript
// 从highlights生成moodboard项
function generateMoodItems(highlights: any[]): Array<{ icon: string; text: string }> {
  if (!highlights || highlights.length === 0) {
    return [
      { icon: '🌊', text: '探索' },
      { icon: '💙', text: '体验' },
      { icon: '🎵', text: '感受' }
    ]
  }
  
  return highlights.slice(0, 3).map((highlight: any) => {
    const text = typeof highlight === 'string' 
      ? highlight 
      : (highlight.title || highlight.description || highlight.feeling || '')
    
    const icon = getIconByKeyword(text)
    const displayText = text.substring(0, 4)
    
    return { icon, text: displayText }
  })
}

// 生成AI反馈模板
function generateAIFeedbackTemplates(intentType?: string): Array<(input: string) => string> {
  if (intentType === 'photography_exploration' || intentType === 'urban_creation') {
    return [
      (input: string) => `"${input}"...这句话里有种特别的光。让我为你扩展这个想法。`,
      (input: string) => `我看到了你的灵感雏形，它正在成形...`,
      (input: string) => `这个想法很美，像是藏在城市缝隙里的故事。`
    ]
  } else if (intentType === 'emotional_healing' || intentType === 'mind_healing') {
    return [
      (input: string) => `我感受到了你文字中的情绪。这里有几种可能的展开方向...`,
      (input: string) => `这个想法很温柔，让我为你找到适合的疗愈体验。`,
      (input: string) => `我看到了你的需要，让我为你设计一段平静的旅程。`
    ]
  }
  // ...更多意图类型
}
```

### 4. imagePoetryGenerator.ts
**职责**: AI视觉诗生成
**关键功能**:
- 图片色调分析
- 情绪识别
- 诗意描述生成
- 标签提取

**关键代码**:
```typescript
export async function generatePhotoPoetry(
  imageUrl: string,
  description: string | null,
  color: string | undefined,
  intentType?: string
): Promise<PoetryResult> {
  // 分析图片色调
  const tone = detectImageTone(color)
  
  // 生成提示语
  const mainPrompt = generateMainPrompt(description, tone)
  const extendedSentence = generateExtendedSentence(description, tone)
  const tags = extractKeywords(description)
  
  return {
    mainPrompt,
    extendedSentence,
    tags
  }
}
```

---

## 功能模块

### 1. 沉浸模式 (Immersion Mode)

**位置**: `ExperienceDay.vue`

**功能**:
- 全屏聚焦体验
- 时间段切换（左右箭头按钮）
- 键盘快捷键支持（← →）
- 禁用页面滚动

**实现**:
```typescript
// 切换到指定时间段
const switchToPeriod = (periodType: 'morning' | 'afternoon' | 'evening') => {
  if (!isImmersionMode.value) return
  
  // 停止当前音频
  if (currentAudio.value) {
    currentAudio.value.pause()
    currentAudio.value = null
  }
  activeSound.value = null
  
  activePeriod.value = periodType
  playSound(SoundType.CLICK)
  
  // 平滑滚动到顶部
  window.scrollTo({ top: 0, behavior: 'smooth' })
}

// 键盘快捷键
const handleKeyPress = (e: KeyboardEvent) => {
  if (!isImmersionMode.value) return
  
  if (e.key === 'ArrowLeft') {
    e.preventDefault()
    navigateToPrevPeriod()
  } else if (e.key === 'ArrowRight') {
    e.preventDefault()
    navigateToNextPeriod()
  }
}
```

### 2. 图片上传和AI视觉诗

**位置**: `ExperienceDay.vue`

**功能**:
- 支持本地图片上传
- Unsplash图片搜索
- AI自动生成诗意描述
- 动态标签提取

**实现**:
```typescript
// 为上传的图片生成AI视觉诗
const generatePoetryForImage = async (imageBase64OrUrl: string, photoData?: UnsplashPhoto): Promise<string> => {
  const poetryResult = await generatePhotoPoetry(
    photoData?.urls?.regular || imageBase64OrUrl,
    photoData?.description,
    photoData?.color,
    travel.value?.data?.detectedIntent?.intentType
  )
  return formatPoetryDisplay(poetryResult)
}

// 处理素材图片上传
const processMaterialImages = async (files: File[]) => {
  for (const file of imageFiles) {
    const base64 = await fileToBase64(file)
    
    // 生成AI视觉诗
    const poetry = await generatePoetryForImage(base64)
    const tags = await extractTagsFromPoetry(base64)
    
    userMaterialImages.value.push({
      url: base64,
      poetry,
      tags,
      isUserUpload: true
    })
  }
}
```

### 3. 动态Moodboard

**位置**: `ExperienceDay.vue`

**功能**:
- 浮动关键词气泡
- 气泡连接线形成探索网络
- 点击气泡展开查看详情
- 支持添加自定义标签

**实现**:
```typescript
// 气泡点击处理
const handleBubbleClick = async (mood: any, index: number) => {
  // 标记为已探索
  exploredNodes.value.add(mood.id)
  
  // 计算气泡位置
  const bubblePos = await getBubbleViewportPosition(index)
  const adjustedPos = adjustExpandedCardPosition(bubblePos)
  
  // 展开气泡并显示子内容
  expandedBubbleId.value = mood.id
  expandedBubbleData.value = { mood, index, position: adjustedPos }
  
  // 如果有图片但没有自动搜索，尝试根据关键词搜索
  if ((!mood.images || mood.images.length === 0) && mood.text) {
    const photos = await searchDestinationPhotos(mood.text, 3)
    const images = photos.map((p: any) => p.urls?.regular || p.url || '').filter(Boolean)
    // 更新气泡的图片
  }
}
```

### 4. 时间段活动

**位置**: `ExperienceDay.vue`

**功能**:
- 自动从 `travel.data` 中提取时间段数据
- 动态生成早晨、午后、夜晚活动
- 支持编辑活动信息
- 图片上传和环境音

**数据结构**:
```typescript
const journeyTimeline = computed(() => {
  const timeline = dayExperience.value?.timeline || []
  return timeline.map((period: any) => ({
    type: period.type,           // 'morning' | 'afternoon' | 'evening'
    timeLabel: period.timeLabel,
    icon: period.icon,
    activities: period.activities.map((activity: any) => ({
      time: activity.time,
      activity: activity.activity,
      narration: activity.narration || activity.aiNarration || ''
    }))
  }))
})
```

---

## 开发指南

### 添加新的意图类型

1. **在 `deepseekAPI.ts` 中更新意图识别**:
```typescript
// 添加新的意图类型
if (input.includes('关键词')) {
  intentType = 'new_intent_type'
}
```

2. **在 `generateInspirationConfig.ts` 中添加配置**:
```typescript
function generateAIFeedbackTemplates(intentType?: string): Array<(input: string) => string> {
  if (intentType === 'new_intent_type') {
    return [
      (input: string) => `针对新意图的反馈模板`,
      // ...
    ]
  }
}
```

### 添加新的时间段活动

1. **更新AI生成逻辑**（在 `deepseekAPI.ts` 中）
2. **更新数据结构**（在 `ExperienceDay.vue` 中）
3. **更新展示逻辑**（确保时间段的正确显示）

### 自定义视觉样式

**全局样式变量**:
```css
:root {
  --bg-gradient-start: #11998e;
  --bg-gradient-end: #38ef7d;
  --card-blur: 20px;
  --border-radius: 24px;
}
```

**组件级别样式**:
每个体验层都有独立的样式类，可以在 `ExperienceDay.vue` 的 `<style scoped>` 中修改。

### 调试技巧

1. **查看AI生成的数据**:
```typescript
console.log('灵感数据:', travel.value?.data)
console.log('动态配置:', travel.value?.data?.inspirationConfig)
```

2. **查看时间段数据**:
```typescript
console.log('时间段:', journeyTimeline.value)
```

3. **查看Moodboard数据**:
```typescript
console.log('动态Moodboard:', dynamicMoodItems.value)
```

---

## 常见问题

### Q: 如何修改时间段的颜色主题？
**A**: 在 `ExperienceDay.vue` 的 `.period-morning`, `.period-afternoon`, `.period-evening` 样式中修改。

### Q: 如何添加新的环境音？
**A**: 在 `configSoundUrls` 对象中添加新的URL映射。

### Q: 如何修改AI视觉诗的生成逻辑？
**A**: 在 `imagePoetryGenerator.ts` 中的 `generatePhotoPoetry` 函数中修改提示语和逻辑。

### Q: 如何自定义气泡的动画效果？
**A**: 在 `ExperienceDay.vue` 的 `.mood-bubble` 样式或 `@keyframes` 中修改动画。

---

## 更新日志

### Version 1.0.0
- ✅ 基础五层体验架构
- ✅ 动态配置生成
- ✅ 沉浸模式
- ✅ AI视觉诗生成
- ✅ 图片上传和Unsplash集成
- ✅ 时间段切换功能

---

## 贡献指南

1. 遵循现有代码风格
2. 添加适当的注释
3. 更新相关文档
4. 测试新功能

---

## 参考资料

- [测试用例](./.vscode/docs/灵感模式测试用例)
- [API文档](./src/services/deepseekAPI.ts)
- [配置文件](./src/config/inspirationConfig.ts)

---

**最后更新**: 2024年

